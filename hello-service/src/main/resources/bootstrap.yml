spring:
  application:
    name: greeter
  cloud:
    config:
      uri: https://config.local.crdant.io:8883

eureka:
  client:
    serviceUrl:
      defaultZone: https://discovery.local.crdant.io:8763/eureka
  instance:
    hostname: greeter.minikube.crdant.io
