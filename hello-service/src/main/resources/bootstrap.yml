spring:
  application:
    name: greeter
  cloud:
    config:
      uri: http://config.gateway-demo.svc

eureka:
  client:
    serviceUrl:
      defaultZone: http://discovery.gateway-demo.svc/eureka
  instance:
    hostname: greeter.gateway-demo.svc
    nonSecurePort: 80
