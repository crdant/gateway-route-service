spring:
  application:
    name: greeter
  cloud:
    config:
      uri: https://config.local.crdant.io:8883

eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka.gateway-demo.svc/eureka
  instance:
    hostname: hello.gateway-demo.svc
    nonSecurePort: 80
