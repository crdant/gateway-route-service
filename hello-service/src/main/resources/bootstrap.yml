server:
  port: 7443
  ssl:
    key-store: /usr/local/etc/certbot/live/local.crdant.io/certificate.p12
    keyStoreType: PKCS12
    keyAlias: local
    key-store-password: numb-avaunt-barbados-pelt

spring:
  application:
    name: greeter
  cloud:
    config:
      uri: https://config.local.crdant.io:8883

eureka:
  client:
    serviceUrl:
      defaultZone: https://discovery.local.crdant.io:8763/eureka
  instance:
    hostname: greeter.local.crdant.io
    secure-port: ${server.port}
    secure-port-enabled: true
    non-secure-port-enabled: false