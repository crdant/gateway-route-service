spring:
  application:
    name: gateway
  cloud:
    config:
      uri: http://config.gateway-demo.svc

eureka:
  client:
    serviceUrl:
      defaultZone: http://discovery.gateway-demo.svc/eureka
  instance:
    hostname: gateway.gateway-demo.svc
    nonSecurePort: 80
