spring:
  application:
    name: gateway
  cloud:
    config:
      uri: http://config.local.crdant.io:8883

eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka.gateway-demo.svc/eureka
  instance:
    hostname: gateway.gateway-demo.svc
    nonSecurePort: 80
