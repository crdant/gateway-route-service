spring:
  application:
    name: gateway
  cloud:
    config:
      uri: http://config.demo.svc

eureka:
  client:
    serviceUrl:
      defaultZone: http://discovery.demo.svc/eureka
  instance:
    hostname: gateway.demo.svc
    nonSecurePort: 80
